Index: mercurial-0.9.2/hgext/hgk.py
===================================================================
--- mercurial-0.9.2.orig/hgext/hgk.py	2006-12-05 23:39:26.000000000 +0100
+++ mercurial-0.9.2/hgext/hgk.py	2006-12-16 15:53:38.000000000 +0100
@@ -280,7 +280,7 @@
     "start interactive history viewer"
     os.chdir(repo.root)
     optstr = ' '.join(['--%s %s' % (k, v) for k, v in opts.iteritems() if v])
-    cmd = ui.config("hgk", "path", "hgk") + " %s %s" % (optstr, " ".join(etc))
+    cmd = ui.config("hgk", "path", "/usr/share/mercurial/hgk") + " %s %s" % (optstr, " ".join(etc))
     ui.debug("running %s\n" % cmd)
     os.system(cmd)
 
